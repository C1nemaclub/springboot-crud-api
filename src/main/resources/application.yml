server:
  port: 4000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/products
    username: cinemacodes
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet